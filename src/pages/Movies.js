export const Movies = () => {
  return (
    <main>
      <h1>Movies page</h1>
    </main>
  );
};

/*

      <ul>
        {movies.map(({ id, name, title }) => {
          return (
            <li key={id}>
              <Link to={`movies/${id}`}>{title ? title : name}</Link>
            </li>
          );
        })}
      </ul>
*/
